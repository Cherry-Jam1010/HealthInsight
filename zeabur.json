{
  "build": {
    "command": "pip install -r requirements.txt",
    "_builder": "nixpacks"
  },
  "start": {
    "command": "uvicorn app.main:app --host 0.0.0.0 --port $PORT",
    "port": 8000
  }
}
